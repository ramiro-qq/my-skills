---
name: example-skill
description: Use this skill as the minimum formal template for new published skills in this repository.
---

# Example Skill

Use this skill as the minimum formal template for new skills in this repository.

## Agent Notes

- Generic behavior: the structure and frontmatter format are shared across Agent Skills consumers.
- Codex-specific note: if a future example mentions a Codex-only tool, label it explicitly and pair it with a plain-language fallback.
- Fallback behavior: when another agent does not support the same tool surface, keep the workflow but replace the tool call with that agent's equivalent file-reading or shell command flow.
