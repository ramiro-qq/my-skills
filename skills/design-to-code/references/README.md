# design-to-code references

## Purpose
These references define the first installable materials for the `design-to-code` skill.

They do not implement the full pipeline yet, but they provide structured guidance and concrete artifacts the installed skill can rely on.

## Included references
- `pipeline.md`: end-to-end pipeline definition
- `acceptance-checklist.md`: validation checklist for output quality
